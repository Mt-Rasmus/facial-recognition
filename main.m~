%{

    TNM034 - Facial Recognition

    Process:
        1. Load image and color correct
        2. Transform to YCbCr Color space and transform chroma
        3. Create face mask
        4. 

%}

%% Main 
clc;
clear;
close all;

% Read image from database
input = imread('images/DB1/db1_09.jpg');
imshow(input);
title('Original');

% Color correct image
output = colorCorrection(input);
%output = refWhite(output);
output = transformChroma(output);

figure
imshow(output);
title('Color corrected');

% Face masking
facemask = detectFace(output);

figure
imshow(facemask);
title('Binary face mask with morphing')

